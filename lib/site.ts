// The canonical origin, in one place. Every canonical URL, sitemap entry,
// structured-data @id and Markdown link is built from this constant: a second
// copy that drifts is how a site ends up ranking a stale host for its own name.
export const SITE_URL = "https://tutelamtl.ca";
